{
  "extends": "./tsconfig.json",
  // Tests are type-checked by `tsconfig.json` but never shipped in the published build.
  "exclude": ["src/**/*.test.ts"],
  "compilerOptions": {
    "noEmit": false,
    "outDir": "dist",
    "rootDir": "src",
    // src imports use explicit `.ts` extensions; rewrite them to `.js` in the emit
    // so the compiled output resolves at runtime (also lifts the allowImportingTsExtensions
    // "noEmit-only" restriction).
    "rewriteRelativeImportExtensions": true,
    "sourceMap": true,
    "declaration": false
  }
}
