# Pix3 Flow — режим «промпт → играбельный прототип → ссылка»

**Статус:** фазы 0 и 1 реализованы и проверены живьём (2026-08-12, ветка `feat/flow-mode`);
фаза 2 (публикация) и фаза 3 (фоновая очередь ассетов) не начаты. Контракт рецептов вынесен в
[flow-recipes-contract.md](flow-recipes-contract.md); журнал живой проверки — §11.
**Дата:** 2026-08-11, ревизия 2026-08-12 (принципы §1.2 — итеративность вместо one-shot,
вопросы к пользователю, точечные правки, контекст-менеджмент, инстант-опыт).
Проверено по коду на `fd46d41`.
**Авторы дизайна:** сессия + Fable (архитектурная часть), развилки и принципы — пользователь.

Цель: путь от пустого экрана до расшаренной играбельной ссылки без открытия «настоящего»
редактора — как в Gemini Canvas / Claude Design, но с полноценным движком под капотом.
Пишем промпт → система собирает играбельную сцену → итерации через чат → один HTML или
ссылка.

Рабочие названия режимов: **Flow** (prompt-first) и **Studio** (существующий полный
редактор).

---

## 1. Рамка, принципы и закрытые развилки

### 1.1 Закрытые развилки (решения пользователя, 2026-08-11)

| Развилка | Решение | Следствие для плана |
| --- | --- | --- |
| LLM-ключи | **BYOK сейчас, серверный metered-прокси потом** | planner-вызов живёт в браузере через существующий `LlmProviderRegistry`; онбординг требует Gemini-ключ или локальный `pix3-agent-bridge`, как Agent-таб сегодня. Metered-прокси — отдельная фаза, вне этого плана |
| Аудитория / публичность | **Приватная бета, только unlisted** | публичный профиль `/u/:username` и модерация UGC — НЕ в v1. Ссылка = секретный slug. Колонка `visibility` в схеме заводится сразу, значение всегда `unlisted` |
| Позиционирование | **Flow — дефолтный первый экран** | welcome перестраивается радикально: prompt-hero это и есть первый экран, списки проектов уезжают во вторичную секцию. Studio = «расширенный режим» |
| Анонимная публикация | **Нет, publish требует логина** | ноль бесхозных артефактов и GC. Download HTML остаётся доступен анониму — это и есть анонимный шаринг |

Прямое следствие решений 2+4: «опубликовать в своём профиле» в v1 реализуется как
приватный список «мои игры» внутри редактора + секретные ссылки. Публичная страница
профиля отложена осознанно, модель данных к ней готова.

### 1.2 Принципы режима (уточнения пользователя, 2026-08-12)

Шесть правил, которые перекрывают любые частные решения ниже. Они сформулированы после
первой версии дизайна и правят её в нескольких местах — там, где это меняло текст,
он переписан.

1. **Цель — НЕ вся игра за один промпт.** Первый ход даёт играбельный скелет и общее
   понимание задумки, дальше — диалог. Успех первого хода измеряется тем, что появилось
   что-то живое, во что можно ткнуть, а не тем, что игра доделана. Это снимает главный
   источник провалов: длинный автономный забег без обратной связи.
2. **Промежуточные состояния играются в уже загруженном рантайме**, а не через сборку
   HTML. Итерация = play mode + hot reload свойств (P0.5, правка видна ≤1 кадра).
   `PlayableHtmlBuildService` вызывается **только** по кнопке Download HTML или Publish.
   Сборка на каждой итерации была бы секундами ожидания на ровном месте.
3. **Агент может спрашивать и уточнять.** Вопрос пользователю — легитимный конец хода, а не
   провал. Промежуточные чекпойнты («сделал управление и врагов — победу считаем по счёту
   или по времени?») лучше, чем угадывание. Механика — тул `ask_user` с вариантами-чипами,
   §5.4.
4. **Только точечные правки файлов.** Полная перезапись существующего файла — исключение,
   требующее причины. Механика уже есть (`str_replace`), не хватает принуждения и того,
   чтобы рецепты состояли из мелких файлов, где точечная правка естественна. §5.4.
5. **Контекст под контролем: измерять, вовремя сжимать, писать доки в проект.** Состояние
   агента живёт не в истории беседы, а в файлах проекта (`design/*.md`). История — расходник,
   который сжимается при достижении ватерлинии. §5.6.
6. **Опыт инстант-приложения при всех возможностях агента.** Ни одного модального ожидания:
   стейдж живёт всегда, ассеты приезжают фоном, ответ агента стримится, тяжёлое (сборка,
   публикация) — только по явной кнопке. Возможности движка не должны ощущаться как
   «тяжёлый редактор, к которому приделали чат».
7. **Вход — не только текст.** В первом же промпте (и в любом последующем) пользователь
   может дать **графические референсы** и **текстовые файлы** — GDD, описание механик,
   таблицу баланса. Это меняет planner на мультимодальный, а стиль ассетов делает
   производным от референса, а не выдуманным. §5.7.

---

## 2. Что уже есть, что расширяем, что пишем с нуля

**Переиспользуется без изменений:**

- Агент целиком: [AgentToolRegistry.ts](../src/services/agent/AgentToolRegistry.ts) (~2660 строк,
  36+ тулов: `create_node`/`add_component`/`set_property`/`fs_write`/`str_replace`/
  `compile_scripts`/`play_start`/`game_input`/`game_observe`/`read_errors`/
  `viewport_screenshot`/`generate_asset`/`process_asset`), скиллы
  `game-prototype`/`verify-and-fix`/`asset-generation`
  ([AgentSkillsService.ts](../src/services/agent/AgentSkillsService.ts)),
  [NodeWatchRecorder.ts](../src/services/agent/NodeWatchRecorder.ts).
- Рантайм монтируется в любой DOM-узел:
  `GamePlaySessionService.registerTabHost(mount)`
  ([GamePlaySessionService.ts:168](../src/services/play/GamePlaySessionService.ts#L168)).
  Игровой канвас **не привязан к Golden Layout** — стейдж Flow это просто ещё один host,
  новой инфраструктуры не требуется.
- Single-file HTML: [PlayableHtmlBuildService.ts](../src/services/export/PlayableHtmlBuildService.ts)
  (инлайн рантайма/three/rapier-wasm/ассетов base64, минификация −43%, `pruneUnusedAssets`,
  size-report) + [ExportPlayableHtmlCommand.ts](../src/features/project/ExportPlayableHtmlCommand.ts).
- OPFS-проекты для анонима: [BrowserProjectStorageService.ts](../src/services/project/BrowserProjectStorageService.ts),
  создание через `ProjectService.createNewProjectWithOptions({ backend: 'browser', templateId })`
  ([ProjectService.ts:660](../src/services/project/ProjectService.ts#L660)).
- Hot reload свойств в play mode (ROADMAP P0.5): правка видна ≤1 кадра **без рестарта** —
  основа быстрой итерации «сделай врагов быстрее».
- Механика шаблонов: `src/templates/projects/<id>/` (`template.yaml` + `files/**` + cover +
  агентский оверлей `.claude/skills`/`AGENTS.md`/`design/`),
  [ProjectTemplateService.ts](../src/services/project/ProjectTemplateService.ts).
- Chat-панель [pix3-agent-chat-panel.ts](../src/ui/agent-chat/pix3-agent-chat-panel.ts) —
  обычный Lit-компонент, GL-специфики нет, встаёт во Flow-шелл как есть.
- **Prompt caching уже реализован**: `cache_control`-breakpoints (system-head / хвост
  tool-списка / последнее сообщение) через `ChatParams.cache`, два измерения кэша
  (reported + предсказанный неизменный префикс) — см.
  [AnthropicLlmProvider.ts](../src/services/llm/AnthropicLlmProvider.ts) и спеку рядом.
  Работа Flow — не «включить кэш», а **сделать префикс стабильным** (рецепт + скиллы +
  system идут первыми и не меняются между итерациями).
- **Счётчик контекста уже есть в UI**: `contextWindow` на модель + отслеживание заполнения
  и cached-сегмента в chat-панели. **Измерение готово, действия нет** — ни сжатия, ни
  тримминга истории в [AgentChatService.ts](../src/services/agent/AgentChatService.ts) не
  существует (§5.6 — новая работа).
- **Вложения в композере уже работают**: картинки и текстовые файлы через paste / drag&drop /
  picker — `ComposerAttachment`, `isTextualFile`, дроп-зона
  ([pix3-agent-chat-panel.ts](../src/ui/agent-chat/pix3-agent-chat-panel.ts), ~строки 65–250,
  585–590). Flow переиспользует эту логику в prompt-hero, а не пишет заново.
- **Стилевые токены по картинке готовы**:
  [AgentVisionService.ts](../src/services/agent/AgentVisionService.ts) резолвит
  vision-модель (даже когда основная модель текстовая) и его system prompt уже заточен под
  «palette hex, rendering style, line/shading, lighting, camera angle, mood» — формат,
  готовый к вставке в generate-промпт. Плюс тул `analyze_image`.
- **Точечное редактирование готово**: `str_replace`
  ([AgentToolRegistry.ts:633](../src/services/agent/AgentToolRegistry.ts#L633)) с описанием,
  прямо предписывающим предпочитать его `fs_write` («a real session regressed a working fix
  that way»). Не хватает **принуждения** (§5.4).
- Remote preview relay (QR, живая игра на устройстве) — `core/preview/` на сервере +
  [StartRemotePreviewCommand.ts](../src/features/project/StartRemotePreviewCommand.ts).
- Библиотека: [library-search.ts](../src/services/library/library-search.ts),
  [LibraryInsertService.ts](../src/services/library/LibraryInsertService.ts),
  [PublishToLibraryService.ts](../src/services/library/PublishToLibraryService.ts).

**Расширяется:**

- [AgentChatService.ts](../src/services/agent/AgentChatService.ts) — verify-нудж (район
  строки 613) превращается в **гейт**; прогресс-события для план-трекера; таймаут запроса;
  свежая беседа на инкремент; prompt-cache breakpoints.
- [pix3-editor-shell.ts](../src/ui/pix3-editor-shell.ts) — ветвление по `workspaceMode`,
  **ленивая** инициализация `LayoutManagerService` (сейчас безусловная, ~строка 165).
- [AppState.ts](../src/state/AppState.ts) — `ui.workspaceMode: 'flow' | 'studio'`.
- [RouterService.ts](../src/services/core/RouterService.ts) — гейт на `isLayoutReady`
  (строка ~82) должен учитывать оба режима; hash-роут `#flow`.
- [pix3-welcome.ts](../src/ui/welcome/pix3-welcome.ts) — prompt-hero как основное
  содержимое (решение: Flow — дефолтный вход).
- `src/templates/projects/` — новые жанровые **рецепты**.

**Пишется с нуля:**

- `src/ui/flow/pix3-flow-shell.ts` (+`.ts.css`) — шелл Flow.
- `src/services/flow/PrototypeBootstrapService.ts` — planner-вызов + детерминированный
  экспандер брифа.
- `src/services/flow/AssetJobQueueService.ts` — фоновая очередь генерации ассетов.
- `packages/pix3-collab-server/src/core/publish/` — публикация (роутер, сервис, storage).
- `src/services/cloud/GamePublishService.ts` + `src/features/project/PublishGameCommand.ts`.

**Явно НЕ используется во Flow:** [EditorTabService.ts](../src/services/editor/EditorTabService.ts)
(1111 строк, целиком GL-зависим) — во Flow нет табов.

---

## 3. Продуктовый сценарий

### 3.1 Первый экран

Prompt-hero: большое поле «Опиши игру» + кнопка Make; 4–6 example-чипов («тапалка: лопай
шарики под таймер», «раннер с препятствиями», «playable ad для match-3 с CTA»); ряд
карточек рецептов (обложки из template-каталога) — клик = «этот жанр + мой промпт».
Существующие проекты — вторичной секцией ниже. Анонимному пользователю ничего не мешает:
проект создастся в OPFS без логина.

**Поле принимает вложения** (принцип 7): картинки-референсы и текстовые документы через
paste / drag&drop / picker — вся страница работает дроп-зоной. Логика переиспользуется из
композера чата (`ComposerAttachment`, см. §2), не пишется заново. Приложенные картинки
показываются миниатюрами с **чипом роли** (стиль / объект / макет), которую можно
переключить до отправки; документы — плашкой с именем и размером. Конвейер разбора — §5.7.

### 3.2 Первый ход: 0 → во что можно ткнуть (цель ≤90 с)

Цель первого хода — **не готовая игра**, а живой скелет + общее понимание задумки
(принцип 1). Всё, что дальше, — диалог.

1. **&lt;5 с** — planner-вызов LLM возвращает `PrototypeBrief` (§5.2). На экране «понимаю
   задумку…».
2. **&lt;3 с** — детерминированный экспандер разворачивает рецепт в OPFS-проект: сцены,
   скрипты, плейсхолдер-арты, перекрашенные в палитру брифа, `design/brief.md` +
   `design/progress.md`. **Игра уже запускается** — это скелет жанра, а не пустота.
3. **Стейдж оживает** — Flow-шелл монтирует рантайм (`registerTabHost`) и стартует play.
   Пользователь трогает болванку своей игры, пока агент работает.
4. **Агент берёт ПЕРВЫЙ инкремент**, не весь список: обычно «управление + core-механика».
   Вместо сырого лога тулов — **план-трекер**: «✓ Управление · ● Враги (проверяю…) ·
   ○ Победа/поражение · ○ Графика». Завершённый инкремент = авто-рестарт стейджа.
5. **Ход заканчивается коротким резюме и развилкой**, а не молчаливым «Готово!»:
   «Скелет играется: стрелки двигают героя, монетки собираются. Дальше могу: (1) добавить
   бомбы, (2) сделать победу по счёту, (3) заменить плейсхолдеры на арт». Три чипа — клик
   вместо печати (принципы 3 и 6).
6. **Ассеты фоном** (§6) — плейсхолдеры подменяются, не блокируя ни чат, ни игру.

**HTML на этом пути не собирается ни разу** (принцип 2): всё играется в уже загруженном
рантайме.

### 3.3 Итерация

Чат слева, стейдж справа. «Сделай врагов быстрее» → `set_property` → hot reload без
рестарта. «Добавь звук при прыжке» → скрипт/behavior + верификация. Одна строка статуса на
тул-вызов, полный лог — по клику.

Три вещи отличают этот цикл от «чата с агентом» в Studio:

- **Агент имеет право спросить.** Развилка, которая изменит структуру («победа по счёту или
  по времени?», «врагов спавнить волнами или постоянно?»), выносится пользователю чипами —
  это дешевле, чем угадать и переделать. Правило балансировки, чтобы не превратиться в
  анкету: спрашивать только там, где варианты дают **разную структуру** сцены или скриптов;
  косметику и параметры выбирать самому и сообщать выбор одной строкой («сделал 3 волны —
  скажи, если не то»).
- **Каждый инкремент завершается играбельным состоянием.** Не «написал систему, потом
  проверю» — а «сделал одну вещь, доказал, показал».
- **Стейдж не гаснет.** Пересборка сцены и рестарт play — это доли секунды в уже загруженном
  рантайме; ощущение приложения, а не билд-пайплайна.

### 3.4 «Структура скрыта до востребования» — эскейп-хатчи по нарастающей

1. **Инспектор-лайт** (фаза 4): клик по объекту на паузе → 3–5 «тюнабельных» свойств,
   помеченных рецептом как `tunables`. Слайдер → hot reload.
2. **Open in Studio** — переключение `workspaceMode`: тот же проект, те же сервисы, тот же
   undo-стек, монтируется полный Golden Layout. Обратно так же мгновенно. Предлагается
   проактивно после 2–3 правок вида «подвинь кнопку чуть левее».
3. Файлы и код — только в Studio. Во Flow их не существует как концепции.

### 3.5 Публикация

Шапка Flow: **Download HTML** (существующая команда, работает анонимно) · **Publish**
(требует логин → секретная ссылка + QR) · **Play on device** (существующий relay для
проверки на телефоне до публикации).

### 3.6 Бюджеты отзывчивости (операционализация принципа 6)

Цифры — проектные цели, по ним меряем регрессии; не все проверены (см. §10.5).

| Момент | Бюджет | Чем достигается |
| --- | --- | --- |
| Промпт → первая реакция UI | &lt;300 мс | оптимистичный переход на Flow-шелл, скелет интерфейса до ответа planner'а |
| Промпт → проект развёрнут | &lt;10 с | planner ≈5 с + детерминированный экспандер ≈3 с |
| Промпт → играбельный кадр | ≤90 с | узкое место — первый компил скриптов рецепта (esbuild.wasm), замерить |
| Правка параметра → видно в игре | ≤1 кадр | hot reload P0.5, **без рестарта** |
| Структурная правка → видно в игре | &lt;2 с | рестарт play в загруженном рантайме, без сборки |
| Сборка HTML | по кнопке | и только по кнопке (принцип 2) |
| Генерация ассета | не блокирует | фоновая очередь + подмена плейсхолдера (§6) |

Правило, следующее из таблицы: **ни одной модалки на пути итерации**. Прогресс-диалоги
допустимы только там, где пользователь сам запросил долгую операцию — Download HTML и
Publish.

### 3.7 Три пользовательских трека

**A — hyper-casual тапалка (маркетолог, без опыта).** «Тапаешь по падающим монеткам,
промахнулся по бомбе — проигрыш». Рецепт `tapper-2d`, скелет играется через ~30 с (серые
кружки падают, тап работает). Агент: перекрасил, добавил бомбы, win по 50 монеткам. Фоном
приехали спрайты. Итерации: «бомб побольше», «звон при сборе». Publish → ссылка коллегам.
Studio не открывался ни разу.

**B — платформер (хоббист).** «Лягушка прыгает по кувшинкам над водой». Рецепт
`platformer-2d` (rapier, прыжок, платформы, `core:CameraBrain`). Ключевая сложность —
левел-дизайн: уровень держится **данными на нодах** (правило скилла «конфиг уровня — в
сцене, не в коде»), агент расставляет платформы `create_node`/`set_property`.
«Первая яма слишком широкая» → hot reload. На третьей итерации «хочу сам подвигать» →
Open in Studio → мышкой → назад во Flow → Download HTML.

**C — playable ad с CTA (продюсер; целевая аудитория ROADMAP 💰).** «Playable для match-3:
5 ходов, потом CTA в стор». Рецепт на базе существующего `playable-2d` (tap-to-start
аудио-гейт + CTA-экран + `PlayableSDK.openStore()` с MRAID/DAPI). Особенность трека —
**бюджет размера**: Flow показывает size-report (уже считается) и предупреждает при
&gt;3–5 MB. Итерации про конверсию: «CTA крупнее и пульсирующей» (`core:PunchScale`).
Валидацию ad-сетей Flow **потребляет**, а не реализует — это ROADMAP P0.6.

---

## 4. Архитектура: Flow как второй shell

**Решение: отдельный shell-режим поверх тех же сервисов.** Отклонены:

- **«Новая панель в Golden Layout»** — не выполняет главное продуктовое требование по
  определению: пользователь видит доки, табы, меню. Agent-панель (`agent-stack` в
  [LayoutManager.ts](../src/core/LayoutManager.ts)) уже есть — это и есть данный вариант, и
  это другой продукт.
- **«Отдельная точка входа, как player.html»** — Flow нужны ВСЕ тяжёлые подсистемы
  (`AgentToolRegistry` тянет `SceneManager`, `CommandDispatcher`, `ScriptCompilerService`
  с esbuild.wasm, `GamePlaySessionService`, image-gen), то есть второй бутстрап DI-графа и
  вечная борьба с дивергенцией. Убийца варианта: **Open in Studio становится перезагрузкой
  страницы** с сериализацией проекта, беседы агента и undo-стека вместо переключения
  компонента. `player-main.ts` не прецедент — он не использует DI-граф редактора вообще.

| | Shell-режим (выбран) | Отдельный entry | Панель |
| --- | --- | --- | --- |
| Стоимость до первого результата | **M** | L | S |
| Риск дивергенции | низкий (один DI-граф) | **высокий** | нулевой |
| Продуктовое качество | полное | полное | **не достигается** |
| Open in Studio | переключение компонента | перезагрузка + handoff | n/a |

Механика: `appState.ui.workspaceMode` → `pix3-editor-shell` рендерит либо `layout-host` +
`LayoutManagerService` (лениво, при первом входе в Studio), либо `<pix3-flow-shell>`.
Flow-шелл = стейдж (свой `div` через `registerTabHost`) + чат (`pix3-agent-chat-panel`) +
шапка (лого → welcome, title, переключатель режимов, Download/Publish/Device) + план-трекер
**сворачиваемым сайдбаром справа от стейджа** (не в шапке — см. ниже).
Все мутации идут через тот же `CommandDispatcher`/`OperationService` — undo/redo и hot reload
работают бесплатно.

Переключение режимов — общий компонент `<pix3-mode-switch>`: в обеих шапках он стоит справа от
названия проекта, всегда виден целиком (сегментированный контрол из двух подписанных половин,
текущий режим — залитая акцентом), ничего не сворачивается в иконку. Ширина постоянна, поэтому
он не сдвигает центрированное название рядом. На экране режимы называются **Studio** и **Vibe**
(внутренний id остаётся `flow` — состояние, хранилище, роут `#flow`).

**План-трекер — сайдбар, а не шапка** (правка по фидбеку 2026-08-12). Шаги — это предложения
(«Tap grid cells to place X marks» + строка доказательства), в ряду чипов они обрезались до
бессмысленных огрызков и давили на идентичность проекта рядом. Теперь это колонка справа от
стейджа (260 px), сворачивающаяся в рейку 38 px с иконкой и счётчиком `3/4`; состояние живёт в
`localStorage` (`pix3.flow.planOpen:v1`). Там же и краткий питч из `design/brief.md` больше **не**
показывается под названием — в Flow строка описания под title убрана совсем.

Пара «название + переключатель» центрирована в обеих шапках: во Flow это средняя колонка сетки
`1fr auto 1fr` (лого слева, действия справа), в Studio — полоса
`--toolbar-side-reserve` над тулбаром. На эту же величину ограничена ширина меню-бара, и хвостовые
секции меню уезжают под кнопку «…», так что столкнуться они не могут.

**Главный технический риск** — скрытые зависимости от `isLayoutReady`/панельных событий.
Найдено грепом: `isLayoutReady` фигурирует в [RouterService.ts](../src/services/core/RouterService.ts),
[LayoutManager.ts](../src/core/LayoutManager.ts), [AppState.ts](../src/state/AppState.ts),
[pix3-editor-shell.ts](../src/ui/pix3-editor-shell.ts) — аудит обязателен при ленивой
инициализации. План Б, если аудит вскроет много завязок: инициализировать LayoutManager
скрыто (нулевой размер / `display:none`) — Flow всё равно получает свой шелл, а стоимость
простаивающего редактора близка к нулю (render-on-demand, см. CLAUDE.md).

**Скриншоты для агента:** `viewport_screenshot` во время play снимает канвас игры
([AgentToolRegistry.ts:846](../src/services/agent/AgentToolRegistry.ts#L846)) — verify-цикл
не зависит от наличия редакторского вьюпорта. Edit-mode скриншоты во Flow v1 недоступны;
если понадобятся — монтировать `src/ui/viewport/editor-tab.ts` скрыто (**не проверено**, что
он монтируется вне GL-контейнера; искать завязки на GL resize-события).

---

## 5. Надёжность: как агент выдаёт играбельное с первого промпта

### 5.1 Что говорят измерения

[agent-eval-results.md](agent-eval-results.md) — готовый natural experiment. Раунд 1:
сборка по GDD с нуля = FAIL (агент пишет скрипты в вакууме, не запускает, обрывается по
капу итераций). Раунд 3: **та же дешёвая модель**, но (а) стартовый шаблон `minigame-2d`,
(б) `game_input`/`game_observe` как инструменты доказательства геймплея, (в) verify-нуджи
харнесса, (г) скилл с engine-ловушками → **S1 и S3 PASS** с доказанным движением и
win/lose. S4 (дизайн-решение) — PASS, но ~35 мин и ~2.1M входных токенов.

Вывод: переход FAIL→PASS дала связка «детерминированный скелет + инструменты доказательства»,
а не смена модели или добавление оркестратора. Отсюда — три слоя ниже.

**Полный декларативный blueprint нод/скриптов отклонён** осознанно: игровая логика не
декларативна без visual-scripting-эквивалента (прямой анти-скоуп ROADMAP §7), а стоимость
контракта «на все жанры» — L+ при недоказанном выигрыше против «рецепт + агент», у которого
выигрыш измерен.

### 5.2 Слой 1 — Рецепты (детерминированные 80%)

Расширение `src/templates/projects/`: `recipe-tapper-2d`, `recipe-runner-2d`,
`recipe-platformer-2d`, `recipe-arena-2d`, `recipe-playable-ad`. Рецепт = обычный шаблон
плюс контракт для агента:

- Полностью **играбельный** микро-скелет: меню → игра → win/lose, управление, счёт.
  Нейтральные плейсхолдер-арты с ролями.
- `design/recipe.md` — машиночитаемая карта: плейсхолдеры и их роли; тюнаблы и где они
  лежат (нода / component config); как добавить новый тип entity; чего НЕ трогать.
- Тюнаблы живут **на нодах и в component config**, чтобы агент (`set_property`) и
  инспектор-лайт били в одно место.
- **Мелкие файлы с одной ответственностью** (принцип 4): `PlayerController.ts`,
  `Spawner.ts`, `ScoreHud.ts`, `GameRules.ts` — не один `GameManager.ts`. Это не стиль, а
  требование надёжности: в монолитном файле любая правка провоцирует полную перезапись, а
  перезапись измеримо теряет предыдущие правки. Ориентир — файл влезает в один экран
  внимания модели (~100–150 строк).
- **Расширяемость важнее полноты.** Рецепт не пытается угадать игру; он даёт каркас и
  **явные точки расширения**, описанные в `recipe.md` («новый тип врага = добавить запись в
  `enemies` + спрайт; спавнер подхватит»). Агенту дешевле дописать в объявленную точку, чем
  изобрести структуру.

Инфраструктура вся готова — работа контентная (написать 3–5 хороших мини-игр), и она
**переиспользуется как обычные шаблоны** в Studio.

### 5.3 Слой 2 — PrototypeBrief (лёгкий IR, один вызов)

`PrototypeBootstrapService` делает один structured-output вызов через существующий
`LlmProviderRegistry`:

```ts
interface PrototypeBrief {
  title: string;
  pitch: string;                    // одна строка для шапки
  recipeId: string;                 // из каталога рецептов
  style: { palette: string[]; artStyle: string; mood: string };  // токены для ВСЕЙ генерации
  entities: Array<{
    role: 'player' | 'enemy' | 'collectible' | 'obstacle' | 'background' | 'ui' | string;
    name: string;
    assetSpec: { prompt: string; sizeHint?: number };
  }>;
  tunables: Record<string, number | string | boolean>;  // маппится на точки рецепта
  winLose: { win: string; lose: string };
  increments: string[];             // 3–5 шагов → design/progress.md
  ctaUrl?: string;                  // только playable-ad
  // Вложения первого промпта (§5.7). Пути — уже сохранённые файлы проекта, не блобы.
  references?: Array<{
    path: string;                   // res://references/... или design/source/...
    kind: 'image' | 'doc';
    role?: 'style' | 'content' | 'layout';   // только для image
    note?: string;                  // что из него взято (токены стиля / раздел GDD)
  }>;
}
```

`style` при наличии style-референса **заполняется из него** (квантованная палитра +
vision-токены), а не выдумывается моделью.

Детерминированный экспандер: создать OPFS-проект из `recipeId`
(`createNewProjectWithOptions`), применить `tunables` через property-schema, tint
плейсхолдеров палитрой (canvas-tint при копировании), записать `design/brief.md` и
`design/progress.md` (формат чек-листа, который скилл `game-prototype` уже требует и умеет
резюмить между сессиями). **Fallback:** planner вернул мусор или неизвестный `recipeId` →
берём ближайший по типу управления рецепт и отдаём промпт агенту как есть; хуже статус-кво
не становится.

### 5.4 Слой 3 — Исполнитель: существующий цикл с proto-пресетом

- **Verify-нудж → verify-гейт.** Сейчас непроверенная мутация геймплея даёт **один** нудж и
  дальше ход закрывается ([AgentChatService.ts:613](../src/services/agent/AgentChatService.ts#L613),
  флаги `unverifiedGameMutation` / `gameVerifyNudged`). Во Flow ход не закрывается, пока:
  (а) `compile_scripts`/`check_scripts` чистые, (б) `read_errors` после `play_start` пуст,
  (в) `game_input`-вердикт по core-механике инкремента пройден — до K попыток, затем
  **честный fail-репорт** («не смог доказать X, вот что видел»). Никогда — молчаливое
  «Готово!».
  **Важное исключение (принцип 3):** гейт НЕ должен ловить ход, который заканчивается
  вопросом к пользователю. Вызов `ask_user` снимает verify-долг так же, как успешный
  `game_input` — иначе агент, честно упёршийся в развилку, будет насильно догоняться в
  бессмысленную верификацию. Реализация: `ask_user` в той же ветке, что очистка
  `unverifiedGameMutation`.
- **Тул `ask_user` — вопрос как легитимный конец хода.** Контракт:
  `ask_user({ question, options?: string[], allowFreeform?: boolean })` → ход завершается,
  UI рисует вопрос и варианты чипами, ответ приходит следующим сообщением пользователя.
  Правило в скилле: спрашивать только про развилки, меняющие **структуру** сцены/скриптов;
  косметику и параметры выбирать самому и сообщать выбор. Без этого правила режим
  выродится в анкету и убьёт «инстант».
- **Точечные правки принудительно (принцип 4).** `str_replace` есть и описан правильно, но
  измерено, что модель всё равно уходит в полную перезапись (eval S4: трижды переписала
  большой файл идентичным содержимым, считая, что поменяла константу). Меры:
  (1) **гард в `fsWrite`** — перезапись СУЩЕСТВУЮЩЕГО файла больше N КБ отклоняется с
  сообщением «используй `str_replace`; для полной замены передай `overwrite:true` и
  причину»; (2) полная перезапись без причины считается сигналом для loop-breaker'а;
  (3) **конструктивная мера, которая работает лучше запретов:** рецепты авторятся из
  МЕЛКИХ файлов с одной ответственностью (`PlayerController.ts`, `Spawner.ts`,
  `ScoreHud.ts`), а не одного `GameManager.ts` — тогда точечная правка естественна, а
  перезапись мелкого файла безвредна. Это требование к рецептам в §5.2.
- **Инкремент — единица работы, и он же граница беседы.** Агент берёт ОДИН инкремент за ход
  (принцип 1), доводит до играбельного и доказанного, отчитывается — и на границе инкремента
  контекст сжимается, а состояние передаётся через доки проекта (§5.6). Измерено (eval,
  advisor-раздел): та же задача, на которой длинная беседа кружила, в свежей беседе с
  компактным брифом решается без подсказок. Это самый дешёвый крупный выигрыш надёжности в
  плане.
- **Прогресс-события** — тул-вызовы мапятся в статусы план-трекера по чек-боксам
  `design/progress.md`; шумные детали сворачиваются.
- Пресет: скилл `game-prototype` инжектится первым, `maxToolIterations` повышен (у дешёвых
  моделей разведка съедает ~15 итераций), «budget your exploration» продублирован в system
  prompt (открытый пункт бэклога раунда 3).

### 5.5 Ранжирование «надёжность на единицу работы»

1. Рецепты — M, выигрыш доказан эмпирически (FAIL→PASS).
2. Verify-гейт — S, механизм готов на ~70%.
3. Инкремент = единица работы + свежая беседа на инкремент — S, атакует измеренную причину
   зацикливания.
4. Brief + экспандер — S/M, убирает «первые 15 итераций разведки» и даёт мгновенный первый
   кадр.
5. `ask_user` + гард на перезапись файлов — S каждый.
6. Embeddings/семантика библиотеки — L, отложено (§6).

### 5.6 Контекст и доки проекта как память агента (принцип 5)

**Что уже есть:** `contextWindow` на модель в провайдерах, отслеживание заполнения контекста
и cached-сегмента в chat-панели, `cache_control`-breakpoints. То есть **измерение**.
**Чего нет:** ни сжатия, ни тримминга истории — `AgentChatService` копит сообщения
неограниченно, пока провайдер не откажет.

**Модель памяти: состояние живёт в файлах проекта, история беседы — расходник.**

| Файл | Содержимое | Кто пишет |
| --- | --- | --- |
| `design/brief.md` | Задумка, жанр, стиль, сущности, win/lose — из `PrototypeBrief` | экспандер, затем агент при смене задумки |
| `design/progress.md` | Чек-лист инкрементов с состоянием; что доказано и чем | агент после каждого инкремента (уже требуется скиллом) |
| `design/decisions.md` | Принятые развилки и ответы пользователя на `ask_user` — чтобы не переспрашивать | агент при закрытии развилки |
| `design/recipe.md` | Карта рецепта: плейсхолдеры, тюнаблы, чего не трогать | автор рецепта (статично) |

**Цикл сжатия:**

1. Заполнение контекста считается из уже доступных `inputTokens` и `contextWindow` модели.
2. **Ватерлиния ~60%** — агенту инжектится «допиши `design/progress.md`, закругляй
   инкремент». Мягкая мера, работы не теряет.
3. **Ватерлиния ~75% или конец инкремента** (что раньше) — **компакция**: агент пишет доки,
   беседа начинается заново с `brief.md` + `progress.md` + `decisions.md` в стартовом
   контексте. Для пользователя это невидимо: тот же тред в UI, разделитель «— контекст
   сжат —».
4. Аварийный порог ~90% — жёсткий тримминг старых tool-результатов (они самые объёмные и
   самые бесполезные задним числом), чтобы ход вообще не упал.

Обоснование ватерлиний, а не «сжимать в конце»: измерено, что источник зацикливания —
загрязнённый контекст, а не сложность задачи (eval, advisor-раздел). Компакция на **границе
инкремента** — это то же самое, что «свежая беседа на инкремент», просто с явным
пределом сверху для длинных инкрементов.

**Взаимодействие с кэшем:** компакция обнуляет prefix-кэш, поэтому она привязана к границе
инкремента, а не к произвольному сообщению. Внутри инкремента префикс (system + скиллы +
`recipe.md` + tool-список) обязан оставаться **байт-в-байт стабильным** — любая
«динамическая» вставка в system prompt (время, счётчики, текущая сцена) убивает кэш и
удорожает прогон в разы. Это требование к proto-пресету, а не пожелание.

### 5.7 Вложения на входе: референсы и документы (принцип 7)

Пользователь может приложить к первому промпту (и к любому последующему) картинки-референсы
и текстовые файлы — GDD, описание механик, таблицу баланса.

**Что уже готово:** композер принимает картинки и текст через paste/drop/picker;
`AgentVisionService` выдаёт по картинке стилевые токены в формате, готовом для
generate-промпта; есть `analyze_image`. **Что делать:** проводка в prompt-hero, персистенция
и мультимодальный planner.

#### Железное правило: вложение немедленно становится файлом проекта

Всё приложенное сохраняется **до** первого вызова модели:

| Вход | Куда | Зачем |
| --- | --- | --- |
| Картинки | `res://references/<имя>` | становятся обычными ассетами: видны в Assets, доступны `generate_asset`/`analyze_image`, переживают перезагрузку |
| Текстовые документы | `design/source/<имя>.md` | первоисточник задумки, читается агентом по секциям |
| Извлечённое | `design/brief.md`, `design/style.md` | нормализованный результат разбора |

Причина жёсткости правила — принцип 5: **всё, что живёт только в истории беседы, исчезает
при компакции.** Референс, приложенный на первом ходу, должен быть доступен агенту на
двадцатом; единственный способ это гарантировать — файл в проекте.

#### Разбор картинок: роль референса решает всё

Урок eval S2 (измерено): генератор **копирует композицию** референса — в спрайт машины
приехала вся трасса с четырьмя машинами. Поэтому картинка не «референс вообще», у неё есть
роль, и роли обрабатываются по-разному:

| Роль | Как используется | Чего НЕ делаем |
| --- | --- | --- |
| **style** (мудборд, скриншот чужой игры) | `AgentVisionService` → токены стиля → `brief.style` → **словами** в каждый generate-промпт | никогда не передаём саму картинку в `references[]` генератора |
| **content** (конкретный объект: «вот такой герой») | картинка идёт в `references[]` генератора как есть — тут копирование композиции желательно | не тянем из неё палитру всей игры |
| **layout** (мокап экрана, схема уровня) | `analyze_image` → структурное описание → подсказка агенту при сборке UI/уровня | не отдаём в генератор ассетов вообще |

Роль определяется автоматически (vision-классификатор в том же вызове, что и токены) и
**показывается пользователю чипом с возможностью переключить** — это дешевле, чем ошибиться
молча, и укладывается в принцип 3.

Палитру берём **детерминированно** — квантованием цветов картинки в браузере, а не «спроси
у модели hex-коды»: быстрее, бесплатно и точнее. Vision-вызов остаётся для того, что
квантованием не берётся (стиль рендера, линия, освещение, настроение).

#### Разбор документов: не инлайнить

GDD на 20 страниц, вставленный в промпт целиком, — это ровно та ошибка, которая в eval S1
сожгла 472K входных токенов (полные референс-доки, пересылаемые каждую итерацию). Поэтому:

1. Документ **пишется в `design/source/`** и не попадает в контекст целиком.
2. Planner получает **бюджетированную выжимку**: заголовки + первые N КБ + всё, что похоже
   на механики/сущности/условия победы. Если документ большой — отдельный дешёвый
   summarize-вызов, результат в `design/brief.md`.
3. Агент дочитывает нужное **по секциям** через `fs_read` — тот же паттерн, что
   `read_skill(id, section)`, и то же правило бюджета разведки в скилле.
4. Противоречия документа и промпта разрешает пользователь через `ask_user`, ответ ложится
   в `design/decisions.md`.

#### Влияние на выбор рецепта

Вложения — это дополнительный вход planner'а, и часто более надёжный, чем короткий промпт:
GDD, где написано «match-3 с ограничением ходов», задаёт `recipeId` точнее, чем фраза
«сделай игру как у конкурентов». Приоритет при конфликте: явный промпт > документ > картинка.

#### Вложения в последующих ходах

Тот же путь без исключений: «вот так должна выглядеть кнопка» на десятом ходу → картинка
падает в `res://references/`, получает роль, и дальше работает как content-референс для
`generate_asset`. Никакой отдельной ветки кода.

---

## 6. Ассеты: placeholder-first, generate-behind

1. **T0:** рецепт содержит нейтральные арты по ролям; экспандер перекрашивает их палитрой
   брифа. Игра сразу выглядит «намеренно», а не серой.
2. **T0+фон:** `AssetJobQueueService` — очередь из `entities[].assetSpec`, конкуренция 2–3,
   каждое задание = существующий пайплайн `generate_asset` (генерация + resize/trim/
   bg-removal уже встроены). По готовности **подмена** через
   `UpdateObjectPropertyOperation` на texture-свойство (undoable, hot reload работает) +
   `viewportRenderService.requestRender()`. Агенту даётся `queue_asset` вместо синхронного
   ожидания — чат не встаёт на 40 секунд, статус приезжает следующей итерацией.
3. **Библиотека прежде генерации, но честно про её размер.** Стор сегодня — 7 items
   ([store-seed.ts](../src/services/library/store-seed.ts)), поиск — substring + токен-AND
   ([library-search.ts](../src/services/library/library-search.ts)). Правило v1: перед
   `queue_asset` — поиск по личной библиотеке и стору по контролируемому словарю
   ролей-тегов (тот же enum, что `entities[].role`); точный тег-матч → вставка через
   `LibraryInsertService`; нет → генерация, и (opt-in) сохранение удачного в личную
   библиотеку с ролью-тегом. Библиотека растёт от использования. Embeddings — до стора в
   сотни items; манифест уже несёт `tags`/`description`/`categoryPath`
   ([library-types.ts](../src/services/library/library-types.ts)), добавится серверной
   фичей без ломки клиента.
4. **Консистентность стиля:** токены `style` инжектятся в каждый generate-промпт. Урок eval
   S2 обязателен: **не передавать полносценные референсы** в `references[]` — генератор
   копирует композицию (в спрайт машины приехала вся трасса с 4 машинами); стиль — словами,
   референс — только первый *сгенерированный* ассет для последующих. Когда пользователь дал
   свои референсы (§5.7), источник токенов — они: style-референс питает `brief.style` и
   через него все генерации, content-референс идёт в `references[]` точечно для СВОЕГО
   ассета и ничьего больше.

---

## 7. Хранение и публикация

### 7.1 Хранение

- **Аноним:** OPFS-проект — ноль запросов, ноль трения, работает сегодня. Риск eviction
  браузером: `navigator.storage.persist()` + мягкий баннер «работа живёт в этом браузере,
  войди чтобы сохранить в облако».
- **Логин просим ровно в трёх местах:** Publish, «Save to cloud», «открыть на другом
  устройстве».
- Привязка OPFS-проекта к аккаунту: есть `CloudProjectService.createProjectFromTemplate` +
  sync-сервисы, но **единой операции «adopt anonymous project» не найдено** — вероятно
  придётся дописать (фаза 2).

### 7.2 Публикация (новая подсистема)

Ключевое упрощение: артефакт = тот самый **single-file HTML** из
`PlayableHtmlBuildService.buildPlayableHtml`. Сервинг тривиален — один файл, никакой
распаковки, никаких ассет-деревьев. Существующий `share_token` в `projects-router.ts` — это
шаринг **collab-проекта**, не игры; переиспользовать нельзя.

**Сервер** — `packages/pix3-collab-server/src/core/publish/` по образцу `core/library/`:

- `published_games (id, owner_id, slug UNIQUE, title, description, visibility, cover_path,
  current_version, created_at, updated_at)` + `published_game_versions (id, game_id,
  version, artifact_path, size_bytes, created_at)`. `visibility` в v1 всегда `unlisted`,
  slug — длинный случайный (секретная ссылка).
- Файлы в `PUBLISH_STORAGE_DIR` (паттерн `LIBRARY_STORAGE_DIR`), артефакт хранится gzip'ом,
  отдаётся с `Content-Encoding: gzip`.
- Роуты: `POST /api/publish` (requireAuth, multipart: html + cover + метаданные, лимит
  ~10–20 MB, квота — например 20 игр × 5 последних версий с подчисткой старых),
  `PATCH /api/publish/:id`, `DELETE /api/publish/:id`, `GET /api/publish/mine`.
- **Сервинг:** `GET /p/:slug` → 302 на `/p/:slug/v/:n` (immutable cache) → HTML.
  **Обязательно отдельный origin** (`play.pix3.dev` на той же машине — cloud+rooms уже
  соседствуют): опубликованный HTML содержит произвольный JS и не должен исполняться на
  origin с auth-куками cloud.pix3.dev. CSP строгая — экспорт уже self-contained, внешних
  запросов не делает. CDN не нужен на старте (один хост + кэш-заголовки); появится трафик —
  CloudFlare перед поддоменом без изменений кода.
- Публичная страница профиля `/u/:username` — **не в v1** (решение: только unlisted).
  Вместо неё `GET /api/publish/mine` + список «мои игры» в редакторе.
- Обложка — скриншот play-канваса при публикации.

**Клиент:** `GamePublishService` (`src/services/cloud/`) + `PublishGameCommand`
(`src/features/project/`, рядом с `ExportPlayableHtmlCommand`): build → прогресс-диалог
(переиспользовать [PlayableExportProgressDialogService.ts](../src/services/export/PlayableExportProgressDialogService.ts))
→ upload → диалог со ссылкой и QR. Кнопка во Flow-шапке и в меню Studio (команда попадёт в
меню автоматически через `CommandRegistry`).

**Preview-relay не трогаем** — он остаётся «живой игрой на устройстве во время итерации»
(требует открытый редактор-host); publish — персистентный снапшот. Переиспользования между
ними нет, и это правильно.

---

## 8. Фазы реализации

### Фаза 0 — закалка харнесса (S/M) — **делать первой**

Во Flow нет судьи-человека, который вмешается; каждый пункт ниже — открытый бэклог eval и
прямая причина «зависаний» у пользователя.

| Работа | Размер | Файлы |
| --- | --- | --- |
| Таймаут LLM-запроса + ретрай (сейчас зависший запрос блокирует ход навсегда) | S | `src/services/llm/`, [AgentChatService.ts](../src/services/agent/AgentChatService.ts) |
| Триггер от loop-breaker'а: «ты застрял → ask_advisor / начни новую беседу». Измерено: модель НИКОГДА не идёт к советнику сама — правило «consult when stuck» мертво без внешнего триггера | S | [AgentChatService.ts](../src/services/agent/AgentChatService.ts) |
| **Контекст-менеджмент (принцип 5):** ватерлинии 60/75/90% от `contextWindow`, компакция на границе инкремента с записью `design/*.md`, аварийный тримминг tool-результатов. Измерение уже есть — нужно действие | **M** | [AgentChatService.ts](../src/services/agent/AgentChatService.ts), [AgentChatHistoryStore.ts](../src/services/agent/AgentChatHistoryStore.ts), [pix3-agent-chat-panel.ts](../src/ui/agent-chat/pix3-agent-chat-panel.ts) (разделитель «контекст сжат») |
| **Тул `ask_user`** + чипы-варианты в чате + снятие verify-долга вопросом (принцип 3) | S/M | [AgentToolRegistry.ts](../src/services/agent/AgentToolRegistry.ts), [AgentChatService.ts](../src/services/agent/AgentChatService.ts), [pix3-agent-chat-panel.ts](../src/ui/agent-chat/pix3-agent-chat-panel.ts) |
| **Гард на полную перезапись** (принцип 4): `fs_write` существующего файла >N КБ требует `overwrite:true` + причину; перезапись без причины — сигнал loop-breaker'у | S | [AgentToolRegistry.ts](../src/services/agent/AgentToolRegistry.ts) (`fsWrite`, ~строка 621) |
| Стабильный кэш-префикс: убрать динамику из system prompt, порядок «system → скиллы → recipe.md → tools». Механизм `cache_control` уже есть — нужна стабильность содержимого | S | [AgentChatService.ts](../src/services/agent/AgentChatService.ts), [AgentSettingsService.ts](../src/services/agent/AgentSettingsService.ts) |
| «Budget your exploration» в system prompt; `text` в live-снапшотах | S | [AgentChatService.ts](../src/services/agent/AgentChatService.ts), [AgentToolRegistry.ts](../src/services/agent/AgentToolRegistry.ts) |

Отдельно от таблицы, но той же фазой по смыслу: **как агент играет в проверяемую игру** —
[agent-gameplay-testing.md](agent-gameplay-testing.md). Там разобрано, почему ввод по
wall-clock-таймеру не масштабируется на динамические игры, и предложены `TimeMode` в рантайме,
`game_run` с покадровыми ассертами (закрывает «проверял с грязной доски» из §11.3 структурно,
а не эвристикой) и бот-политика. Фазы 1–2 того плана — прямое продолжение этой.

**Критерий:** прогон S1/S3 из [agent-eval-scenarios.md](agent-eval-scenarios.md) **без
вмешательств судьи**; беседа переживает компакцию без потери задачи (проверяется прогоном,
где контекст заведомо переполняется); ни одной полной перезаписи файла >N КБ за прогон;
входные токены снижены измеримо (замер до/после — cache-hit и суммарный in).
**Зачем первой:** без этого фаза 1 демонстрирует продукт, который зависает, молча сдаётся,
теряет свои же правки при перезаписи файла и упирается в потолок контекста на третьей
итерации. Все пять пунктов — общая польза: они улучшают и Agent-таб в Studio.

### Фаза 1 — вертикальный срез «промпт → играется → скачал HTML» (L, демонстрируема)

| Работа | Размер | Файлы |
| --- | --- | --- |
| `workspaceMode` в AppState + hash-роут + ленивый LayoutManager + аудит `isLayoutReady` | S/M | [AppState.ts](../src/state/AppState.ts), [pix3-editor-shell.ts](../src/ui/pix3-editor-shell.ts), [LayoutManager.ts](../src/core/LayoutManager.ts), [RouterService.ts](../src/services/core/RouterService.ts) |
| `pix3-flow-shell` (стейдж через `registerTabHost`, чат = существующая панель, шапка, план-трекер) | M | новое `src/ui/flow/pix3-flow-shell.ts` + `.ts.css` |
| Prompt-hero как первый экран (Flow — дефолтный вход) | S/M | [pix3-welcome.ts](../src/ui/welcome/pix3-welcome.ts) |
| **Вложения в hero** (принцип 7): вынести attachment-логику композера в общий модуль, дроп-зона на всю страницу, миниатюры + чип роли | S/M | [pix3-agent-chat-panel.ts](../src/ui/agent-chat/pix3-agent-chat-panel.ts) → новое `src/ui/shared/composer-attachments.ts`, [pix3-welcome.ts](../src/ui/welcome/pix3-welcome.ts) |
| **Конвейер вложений** (§5.7): персистенция в `res://references/` + `design/source/`, vision-классификация роли, квантование палитры, бюджетированная выжимка документов, мультимодальный planner | M | `src/services/flow/PrototypeBootstrapService.ts`, [AgentVisionService.ts](../src/services/agent/AgentVisionService.ts) (переиспользуется), `src/services/image-gen/image-ops.ts` (**новая** функция извлечения палитры — квантования там сейчас нет, но есть `ImagePixels`/`samplePixelColor` и опыт chroma-key из Sprite Editor) |
| 2 рецепта: `recipe-tapper-2d`, `recipe-runner-2d` | M (контент) | `src/templates/projects/recipe-*/` |
| `PrototypeBootstrapService`: planner-вызов + экспандер + fallback | M | новое `src/services/flow/PrototypeBootstrapService.ts` |
| Proto-пресет агента: verify-**гейт**, инкремент как единица хода, поднятый кап, прогресс-события | M | [AgentChatService.ts](../src/services/agent/AgentChatService.ts), [AgentSettingsService.ts](../src/services/agent/AgentSettingsService.ts) |
| Download HTML в шапке (единственное место, где вообще собирается HTML) | S | вызов [ExportPlayableHtmlCommand.ts](../src/features/project/ExportPlayableHtmlCommand.ts) |

**Критерий готовности** (сформулирован под принцип 1 — не «игра за один промпт»):
с чистого браузера, без логина, с BYOK-ключом, промпт «тапалка про монетки»:

1. ≤90 с до первого играбельного кадра на стейдже;
2. первый ход закрывается **одним** доказанным инкрементом (`game_input`-вердикт виден в
   логе) + резюме с 2–3 вариантами продолжения чипами;
3. три последующие итерации в диалоге («добавь бомбы», «победа по 50 монеткам», «сделай
   быстрее») доводят игру до win/lose — **ни одной сборки HTML** за весь путь, всё в
   загруженном рантайме;
4. на пути есть минимум один `ask_user` на структурной развилке, и ответ пользователя
   попадает в `design/decisions.md`;
5. Download HTML в конце открывается и играется.

Плюс отдельный прогон **с вложениями** (принцип 7): тот же промпт + мудборд-картинка +
однострочный GDD → палитра игры видимо совпадает с мудбордом, картинка лежит в
`res://references/`, документ в `design/source/`, ни один из них не инлайнится в контекст
целиком, и оба переживают компакцию (проверяется вопросом агенту после сжатия).

Regression: Studio не изменился (существующие layout-тесты + ручной прогон).
**Риски:** скрытые GL-зависимости (план Б — скрытая инициализация LayoutManager);
латентность/надёжность structured-output у доступных провайдеров для `PrototypeBrief`
(Gemini умеет; бридж-провайдеры проверить, иначе нужен текстовый fallback-парсер); тайминг
90 с — оценка, узкое место — первый компил скриптов рецепта (esbuild.wasm) + planner-вызов,
надо замерить.

### Фаза 1.5 — «вау-пол» T0 (M/L) — контракт в [flow-wow-t0.md](flow-wow-t0.md)

Разбор 2026-08-13: вау-разрыв с one-shot HTML-генераторами не модельный, а
бюджетно-архитектурный — ~70% их вау это статическая косметика, которую надо печь в
рецепты (ноль токенов), а сочность делать one-liner'ами движка. Три блока: (A)
рантайм-примитивы `scene.juice.burst`/`floatText`, `scene.audio.sfx` (синт-пресеты без
ассетов), glow/outline у `Label2D`; (B) неон-переделка `recipe-bouncer-2d` — градиентный
фон, `PostProcess` с `affect2D: true` (bloom на 2D уже работает), glow-HUD, звук/частицы/
попапы в `TouchRules`; (C) планнер — `theme` + style-паки экспандера, `increments[0]` =
отличие от скелета (не «controls + core loop»), секция `wow` в брифе.
**Критерий:** T0 bouncer'а выглядит «дизайнерски» без единого хода агента; контрольный
прогон — «сделай неоновый пинбол».
Решение против HTML/JS-референсов перед движковой версией — осознанное: двойная стоимость
в чужой парадигме + продуктовый риск («движок — это где становится хуже»).

### Фаза 2 — публикация (M)

Сервер `core/publish/` + сервинг на отдельном origin + `GamePublishService` +
`PublishGameCommand` + диалог со ссылкой/QR + «войди чтобы опубликовать» + adopt
OPFS-проекта в облако.
**Критерий:** publish из Flow → ссылка играется в инкогнито; повторный publish создаёт
версию; лимиты и удаление работают; артефакт отдаётся не с origin авторизации.
**Риски:** безопасность UGC-HTML (sandbox-origin решить ДО запуска, не после); диск на
общем хосте (квоты сразу, см. cloud-дашборд).

### Фаза 3 — ассет-конвейер и рецепты (M/L)

`AssetJobQueueService` + тул `queue_asset` + подмена плейсхолдеров; +3 рецепта
(`platformer-2d`, `arena-2d`, `playable-ad`); поиск по библиотеке по ролям-тегам перед
генерацией; opt-in сохранение удачных генераций в личную библиотеку.
**Критерий:** трек A (§3.7) проходит без единого ожидания генерации в чате; спрайты стилево
консистентны (ручная оценка по 5 прогонам).

### Фаза 4 — полировка опыта (M)

Стриминг дельт в чат (интерфейс `onDelta` в `LlmTypes.ts` заложен, но провайдеры его могут
игнорировать и панель дельты не рендерит — стриминга в UI фактически нет); инспектор-лайт
по тюнаблам рецепта; remix/fork опубликованной игры (копия проекта, не HTML);
проактивные подсказки «Open in Studio».

Почему стриминг не в фазе 1, хотя принцип 6 — про «инстант»: ощущение мгновенности в фазе 1
даёт **живой стейдж через ~10 секунд** и план-трекер, а не посимвольный вывод текста.
Пользователь в этот момент тыкает в свою игру, а не читает чат. Стриминг — полировка
восприятия, а не источник отзывчивости, и он зависит от поддержки у конкретных провайдеров
(риск уйти в отладку чужих SSE вместо продукта).

### Отложено за пределы плана

Серверный metered LLM-прокси (решение: BYOK сейчас); публичный профиль `/u/:username` и
модерация UGC (решение: unlisted-бета).

---

## 9. Анти-скоуп (осознанно не делаем)

Полный декларативный game-blueprint; visual scripting (прямой анти-скоуп ROADMAP §7);
embeddings-поиск по библиотеке; собственный CDN; соцфичи профиля (лайки/комментарии);
мультиплеер во Flow; мобильный редактор; автоматическая валидация ad-сетей (ROADMAP P0.6,
отдельный трек); realtime-collab редактирование Flow-проектов; анонимная публикация.

---

## 10. Что проверить руками до/во время фазы 1

1. Монтируется ли [editor-tab.ts](../src/ui/viewport/editor-tab.ts) вне Golden Layout
   (для скрытого edit-вьюпорта во Flow). Для среза 1 можно жить без него — play-канвас
   покрывает verify-цикл.
2. Полный аудит подписчиков на `isLayoutReady` и панельные события при ленивой
   инициализации LayoutManager.
3. Сценарий «привязать существующий OPFS-проект к облачному аккаунту» — sync-сервисы есть,
   единой adopt-операции не найдено.
4. Structured-output у бридж-провайдеров для `PrototypeBrief` (латентность + надёжность).
5. Замер «≤90 с до играбельного» на OPFS-проекте: planner-вызов + первый компил
   esbuild.wasm.
6. Мультимодальность planner'а у доступных провайдеров: `AgentVisionService` резолвит
   vision-модель отдельно от основной — надо решить, идёт ли planner-вызов с картинками
   через него (тогда brief собирается в два шага: токены → бриф) или напрямую в
   vision-capable модель. Второе проще, но требует, чтобы основная модель умела картинки.
7. Насколько надёжно vision-классификация роли референса (стиль / объект / макет) — если
   промахивается часто, оставить только чип с ручным выбором и дефолт `style`.

---

## 11. Журнал реализации и живой проверки (2026-08-12)

Ветка `feat/flow-mode`. Проверка — режим прогонялся целиком через свой же UI на подписочной
модели (Claude Sonnet 5 через `pix3-agent-bridge`), два прототипа собраны с нуля.

### 11.1 Что реализовано

**Фаза 0 (закалка харнесса, польза и для Studio):** таймаут запроса к провайдеру (180 с,
цепочечный abort, один ретрай); эскалация loop-breaker'а к `ask_advisor` по трём сигналам
(повтор идентичного вызова, серия ошибок, форсированные перезаписи); тул `ask_user` с чипами,
завершающий ход и снимающий verify-долг; гард на полную перезапись файла >2 КБ
(`overwrite` + `reason`); ватерлинии контекста 60/75/90 % со сжатием в `design/*.md`;
`text` в снапшотах нод; правило про бюджет разведки.

**Фаза 1:** `appState.ui.workspaceMode` + `WorkspaceModeService` (память режима на проект,
роут `#flow`), `pix3-flow-shell` (чат + всегда живой стейдж + план-трекер из
`design/progress.md` + сплиттер), ленивый Golden Layout, `play-workspace` (play-команды без
табов), `PrototypeBootstrapService` (planner + детерминированный экспандер), prompt-hero с
вложениями, три рецепта + промоушен `playable-2d`, спека против дрейфа контракта.

**Flow-пресет агента:** скилл `flow-increment`, `design/recipe.md` в кэшируемом префиксе,
verify-гейт на 3 попытки, поднятый пол итераций, гарантированный финальный ответ при упоре
в кап (ход с выключенными тулами).

### 11.2 Баги, найденные догфудингом (все починены)

| Симптом | Корень |
| --- | --- |
| Новый проект открыл сцену предыдущего | id сцены выводится из пути (`scenes-main` у всех), а дескрипторы прошлого проекта не чистились при создании нового |
| Скрипты нового проекта не компилировались | компиляция триггерилась только переходом статуса в `ready`; создание проекта поверх открытого статус не меняет |
| Компонент терял дефолты своего класса | `SceneLoader` **заменял** конфиг вместо слияния: поле, которого нет в YAML, приходило `undefined` (шаг сетки 100 становился 1) |
| Агент жёг ~9 итераций на stop/start | `play_start` возвращал `ok:false`, когда игра уже идёт, — а во Flow это норма |
| Агент гадал после неудачного `set_property` | тул возвращал голое `{ok:false}` без причины |
| Ход упирался в кап молча | нет финального текстового ответа |
| Агент чинил уже починенное | `read_errors` отдавал протухшие диагностики компилятора |
| «Добавьте API-ключ» при рабочем мосте | проверка ключа обгоняла регистрацию провайдеров моста |
| Игра растянута не в своё соотношение | стейдж уважал скрытую Studio-настройку вместо авторского вьюпорта |
| Панель чата наезжала на стейдж | `min-width: auto` у флекс-элемента панели |
| Проект уезжал в Studio сразу после создания | режим выбирается до того, как у проекта появляется id |

**Второй заход догфудинга (крестики-нолики, тот же день).** Ход на один инкремент сжёг весь
кап в 40 итераций; разбор транскрипта дал ещё три бага.

| Симптом | Корень |
| --- | --- |
| ~6 итераций и оба срабатывания loop-breaker'а на цикле compile→check→fix→check | `compile_scripts` (esbuild, только транспиляция) возвращал `ok:true, warnings:[]` при трёх ошибках типов — узнать о них можно было лишь отдельным `check_scripts`. Теперь компиляция сама гоняет тайп-чек и отдаёт диагностику; `check_scripts` остался для перепроверки нетронутого кода |
| Тап по имени мог уйти в другую ноду | `SceneRunner.findLiveNodeByName` обходил дерево стеком с `push` по порядку и `pop` — то есть **обратным** pre-order, вопреки собственному комментарию «first match in DFS order». При дублирующемся имени выигрывала **последняя** нода — ровно та, которую агент бросает в конце родителя |
| Дубликат имени резолвился молча | резолв по имени не сообщал о неоднозначности. Добавлены `findLiveNodesByName` и поля `ambiguousTargets` (`game_input`) / `hint` (`game_observe`) |

**Третий заход (крестики-нолики на Haiku 4.5).** После фикса компиляции ход на инкремент
сжался с 40 итераций до 17, `check_scripts` агент больше не зовёт. Ещё четыре бага:

| Симптом | Корень |
| --- | --- |
| Скрипт ставит `btn.label = 'X'`, а на экране пусто — при этом `game_observe.text` бодро отдаёт `X` | `label` и остальные `label*` у `UIControl2D` были простыми полями. Инспектор писал их через схему, где `setValue` руками дёргал `updateLabel()`, поэтому авторинг работал; присвоение из скрипта меняло поле и не перерисовывало канвас-текстуру, а `getDisplayText()` (и через него `text` в наблюдении) читал то же поле. **Проверка состоянием давала ложноположительный результат — баг увидел глазами пользователь, а не агент и не я.** Поля стали аксессорами с гардом на равенство: перерисовка ровно при изменении |
| Протухшие диагностики всё ещё вылезали в `read_errors` после чистого тайп-чека | `SCRIPT_DIAGNOSTIC_PATTERN` якорился на начало строки (`^`), а диагностики попадают в кольцевой буфер через Logger с префиксом `[Pix3 ERROR] `. Юнит-тест кормил голую строку и проходил, в реальном редакторе не ретайрилось ничего. Паттерн раз-якорен, разделитель ` — ` оставлен как признак диагностики |
| Loop-breaker трижды ругнулся на «идентичный `play_start`» и эскалировал до «ты застрял» | память повторов не сбрасывалась после мутации. Между вызовами были `str_replace` + `compile_scripts`, то есть повтор был законным (перезапуск, чтобы подхватить свежую сборку — ровно то, что предписывает описание тула). Добавлен `STATE_CHANGING_TOOLS`: успешная мутация забывает чужие сигнатуры, свою оставляет |
| Правку скриптом через `str_replace` verify-гейт не замечал | `isGameLogicMutation` знал только `fs_write`, хотя скиллы велят предпочитать `str_replace`. Целый инкремент можно было собрать и закрыть ход без доказательства в игре |

### 11.3 Наблюдения, не закрытые кодом

- `scene.time.hitstop`, вызываемый каждый кадр из `onUpdate` при длящемся оверлапе, оставляет
  игру замороженной. `GameTime` сам по себе корректен (берёт максимум, тикает по реальному
  времени), воспроизвести изолированно не удалось — записано как наблюдение, не как фикс.
- `design/recipe.md` вышли по 5–6.7 КБ против целевых 4 КБ.
- Верификация «сочности» состоянием работает (пик `scaleDelta` виден), но агент склонен
  тянуться к скриншотам — правило добавлено в скилл, эффект замерен только качественно.
- ~~**Разведка всё ещё дорогая.**~~ **Закрыто, см. 11.6.** В ходе по крестикам-ноликам 16 из 40
  итераций ушли на чтение до первой правки, включая `fs_read scenes/main.pix3scene` **шесть раз**
  страницами (файл 15.4 КБ): якоря `str_replace` съезжают после каждой правки, и агент
  перечитывает файл, чтобы найти следующий. `design/recipe.md` при этом не прочитан ни разу — он
  в кэшируемом префиксе, но агент всё равно перевывел ту же карту нод из YAML.
- ~~**Брошенный `create_node` оставляет мусор в сцене.**~~ **Закрыто, см. 11.6.** Агент создал
  `Button2D`, назвал его `cell-0` при уже существующем `cell-0`, потом передумал и стал править
  YAML — нода осталась висеть последним ребёнком `Board`. Резолв по имени это больше не путает
  (см. 11.2), но дубликат имени среди сиблингов никто не проверял на создании.
- **Агент дважды объявил RETRY починенным, не починив.** В последней «проверке» его `before`-снимок
  уже содержал `text: "X"` — ряд собирался на грязной доске, и мусор был прочитан как успех.
  Харнесс никак не отмечает, что наблюдение началось из конечного состояния; кандидат на фикс —
  предупреждать, когда `before` уже удовлетворяет проверяемому условию. Правило «проверяй с чистого
  старта» добавлено в `flow-increment`. **Корень RETRY найден** (см. 11.4): кнопки не было на
  экране — скрипт показывал детей `Result Overlay`, оставляя сам оверлей `visible: false`.
- ~~**Контракт рецепта про RETRY нигде не сказан.**~~ **Закрыто, см. 11.6.** Финальный корень мёртвой кнопки: `GameRules`
  привязывает обработчик и оставляет кнопку `enabled: false`, включая её в своём собственном
  game-over-пути (`setButtonEnabled(retryButton, true)`). Агент реализовал финал сам и показал
  оверлей вручную, минуя этот путь — кнопка на экране, обработчик привязан, `enabled: false`.
  Ни `design/recipe.md`, ни скилл об этом владении не сообщали. Кандидат на фикс — описывать в
  рецепте, что оверлей результата и RETRY принадлежат `GameRules`, и как вклиниться правильно.
  Диагностика теперь одноходовая: `game_observe` отдаёт `control: {enabled, hovering, pressed}`.
- ~~**Отладочные хэндлы протухают на первой сцене.**~~ **Не воспроизводится (перепроверено
  2026-08-12).** `__PIX3_DEBUG__.liveScene()` и `__PIX3_RUNTIME_SCENE__` якобы продолжали отдавать
  меню-сцену, пока игра шла в `main`. Живая перепроверка на том же проекте (тап по `Menu Button`,
  переход `main` → `menu`) даёт корректные корни сразу после перехода: `Game Root` → `Menu Root`.
  Механизм это подтверждает — `registerRuntimeSceneRoot(this.scene)` зовётся в `runGraph` на
  каждый старт сцены, а сам `THREE.Scene` переиспользуется через `clear()`. Скорее всего в тот раз
  наблюдалась именно `scene_tree`-половина (она читает авторский граф) — её и починили, см. 11.6.
- **Два tsconfig'а расходятся в семантике полей классов.** Корневой (`useDefineForClassFields:
  false`) валидирует `packages/pix3-runtime/src`, а `packages/pix3-runtime/tsconfig.json` (`true`)
  собирает его. При define-семантике объявление поля в подклассе создаёт own-property, перекрывающую
  аксессор базы — а базы теперь их имеют (`UIControl2D.label*`). Сейчас ни один подкласс имён не
  переобъявляет, поэтому не стреляет; менять семантику сборки без отдельной проверки (сборка +
  DeepCore) не стал.
- ~~**Переход между сценами читается как «ничего не произошло».**~~ **Закрыто, см. 11.6.** Тап по
  `Menu Button` увёл игру в меню, но `game_input` вернул `NO ACTIVITY`: наблюдаемая нода исчезла
  вместе со сценой, и вердикт сообщил про отсутствие реакции вместо смены сцены. Плюс `scene_tree`
  продолжал показывать прежнюю сцену (он читает активный граф редактора, не рантайм) — вдвоём эти
  два сигнала уводят в неверный вывод, что подтвердилось на мне.

### 11.4 Системный класс: «инспектор перерисовывает, присвоение из скрипта — нет»

Баг с невидимым `label` оказался не единичным. Аудит всех `getPropertySchema()` в рантайме дал
**31 свойство в 16 типах нод** с той же формой: свойство хранится простым публичным полем, а
`setValue` в схеме делает настоящую работу (кламп, пересборка геометрии, перерисовка канваса,
цвет в материал). Инспектор зовёт `setValue` — авторинг работает. Скрипт пишет поле — не
перерисовывается ничего, а геттер возвращает новое значение, поэтому **проверка состоянием
подтверждает успех, которого на экране нет**. Самые больные: `Bar2D.value` (полоска HP не
двигается), `Slider2D.value` и `AnimatedSprite2D.currentClip` (у обоих метод приватный — у скрипта
вообще нет рабочего написания), `Checkbox2D.checked`, `InventorySlot2D.quantity`,
`SpineSkeleton2D.color` (тинт не применялся никогда), `Button2D.width/height` (двигали зону клика,
но не картинку).

**Механизм:** `fw/reactive-schema-properties.ts` — `installReactiveSchemaProperties(this,
X.getPropertySchema)` последней строкой конструктора превращает каждое схемное свойство,
хранимое простым полем, в аксессор, направляющий запись через `setValue`. Установка **по
экземпляру**, а не на прототип: при `useDefineForClassFields: true` объявление поля перекрыло бы
прототипный аксессор, а сорсы компилируют потребители своими конфигами. Три тонкости, каждая
найдена на реальном падении:

1. **Рекурсия.** Большинство `setValue` сами пишут своё же свойство (`Bar2D.setValue`), поэтому
   пока `setValue` в работе, запись в это свойство только запоминает значение.
2. **`getValue` — не всегда честное чтение поля.** `Button2D.textureHover` подставляет
   `{type:'texture', url:''}` вместо `null`, чтобы пикеру инспектора было что показать. Дописывание
   результата обратно превращало очищенный ref в truthy — и `SceneSaver` сериализовал пустышку.
   Теперь: если `setValue` присвоил поле сам, это значение и есть истина.
3. **Объектные поля мутируются на месте.** `this.tileScale.set(x, y)`, `this.groups.add(g)` — а
   геттер отдаёт снимок (`{x,y}`, массив). Дописывание снимка подменяло живой объект мёртвой копией,
   и следующий `.set()`/`.add()` падал. Поэтому обратно читается только примитив.

Плюс эскейп-хатч `assignWithoutSchemaRefresh` для методов вида «сделал работу → записал поле»:
`SpineSkeleton2D.play(name, {mix})` иначе перезапускал трек с авторскими опциями, затирая
переданные вызывающим.

**Охват:** 18 типов нод. Отказы обоснованы: `InstancedMesh3D` —
поля `readonly`, а мутабельная поверхность `node.mesh` и так работает; `AnimationPlayerBehavior` —
значения живут в `config.*`, не полями; базы (`Node2D`/`Node3D`/`Group2D`) — установка отработала бы
до появления полей подклассов, а их собственные свойства не того класса.

**Страж от дрейфа:** `fw/reactive-schema-coverage.spec.ts` конструирует каждый тип, который умеет
собирать `SceneLoader`, и падает, если класс не позвал установщик — плюс сверяется со списком
`case`-меток в `SceneLoader`, так что новая нода не проскочит незамеченной.

**Попутно найденный настоящий баг рендера:** `InventorySlot2D.updateQuantityDisplay` при уже
существующем меше не присваивал новую текстуру материалу — бейдж навсегда сэмплил первую (уже
уничтоженную), то есть число на экране не менялось после первой отрисовки. Починено.

Чего механизм не лечит: мутацию `readonly`-вектора на месте (`sprite.tileOffset.x += dt`) — там
присвоения не происходит, перехватывать нечего. Для таких случаев нужен метод.

### 11.5 Что получилось из прототипов

- **Змейка** (`recipe-arena-2d`): пошаговое движение по сетке 100 px, рост хвоста сегментами,
  яблоки в клетках, смерть о стену → GAME OVER → RETRY (перезапуск проверен состоянием).
- **Pocket Pinball** (`recipe-bouncer-2d`): два флиппера с вращением вокруг внутреннего конца
  на стрелках/A-D, бамперы со счётом, дрейн и три жизни, шейк на потере жизни, punch-scale на
  счёте (подтверждён пиком `scaleDelta ±0.185`).

Оба собраны без единой сборки HTML — всё в загруженном рантайме, как требует принцип 2.

---

## 11.6 Закрытие наблюдений §11.3 (2026-08-12, вторая сессия дня)

Пять пунктов из §11.3 закрыты кодом; каждый проверен в живом редакторе через мост
(проект «Tic-Tac-Toe Duel», тот самый, на котором их и нашли).

| Наблюдение | Что сделано |
| --- | --- |
| Разведка: шесть страничных перечиток одной сцены | `str_replace` при успехе возвращает `context` — **окрестность правки в уже обновлённом файле** (±8 строк, байт-точно, с `startLine`/`endLine`) плюс новый `totalLines`. Якорь для следующей правки приходит вместе с предыдущей, перечитывать нечего. Если сама правка не влезает в 2 КБ, контекст **опускается**, а не режется: обрезанный кусок нельзя копировать в `old_string`, и лучше его не давать вовсе |
| То же, вторая половина | `fs_read` на файле **до 16 000 символов игнорирует запрошенный диапазон** и отдаёт файл целиком с `note`. Живая проверка: `scenes/main.pix3scene` = 14 189 символов, 481 строка — приходит одним куском (порог с запасом ниже `MAX_TOOL_RESULT_CHARS` = 24 000 даже с JSON-экранированием) |
| Дубликат имени на `create_node` | Создание не блокируется (дубликаты легальны), но результат несёт `warning` + `duplicateNameNodeIds`: адресация по имени в `game_input`/`game_observe` стала неоднозначной — переименуй или удали |
| Переход между сценами = «ничего не произошло» | `GameInputService` сравнивает корни живой сцены **по идентичности объектов** до и после окна (`runGraph` пересобирает клон, поэтому свап ловится и при возврате в ту же сцену). Появились поле `sceneChanged: {fromRoots,toRoots}` и вердикт `SCENE CHANGED: …`, который **лидирует** над всеми остальными сигналами и прямо говорит, что дельты наблюдаемых нод описывают покойников. Вердикт теперь выдаётся и когда не наблюдали ничего — свап сам по себе реакция |
| `scene_tree` молчал про расхождение | Пока идёт play и живые корни не пересекаются с авторскими, `scene_tree` отдаёт `staleWhilePlaying` с именами живых корней и отсылкой к `game_observe` |
| Контракт RETRY/`GameRules` | Раздел «Ending a run belongs to GameRules» добавлен в `design/recipe.md` всех трёх рецептов: оверлей и RETRY принадлежат `GameRules`, свой финал делается вызовом `finish(won)`, а не показом оверлея руками; признак нарушения — `control: { enabled: false }` в `game_observe` |

**Живая проверка** (после жёсткой перезагрузки, тап по `Menu Button` в `main`):
`sceneChanged: {fromRoots:["Game Root"], toRoots:["Menu Root"]}`, вердикт начинается с
`SCENE CHANGED`, `scene_tree` вернул `staleWhilePlaying: "…live roots: Menu Root…"`.

## 11.7 Приёмочный прогон фазы 1 (2026-08-12, третья сессия): первый кадр

Прогон критерия «≤90 с до первого играбельного кадра» с промптом «тапалка про монетки» на
подписочной модели через мост. Первый же заход вскрыл, что **первым кадром продукта была красная
ошибка**, и разбор дал три независимых бага — все с общим корнем «состояние врёт, а на это
состояние все опираются».

| Симптом | Корень | Что сделано |
| --- | --- | --- |
| На стейдже `Cannot start the game: no active scene is open.` вместо игры; баннер висит минутами, пока агент сам не перезапустит игру | **Play-режим переживал смену проекта.** Игра предыдущего проекта продолжала «идти» (`isPlaying: true`); когда промпт создавал новый проект, `clearOpenDocumentState` сносил сцены, а регистрация нового стейджа как хоста заставляла `GamePlaySessionService` переподключить рантайм — к проекту, у которого сцены ещё нет | `clearOpenDocumentState` гасит play-режим вместе с документами: игра не может продолжаться в проект, где её сцен больше нет |
| После неудачного старта состояние утверждало, что игра идёт: `play_status → playing`, `play_start → ok`, а рантайма нет | `startRuntime` при пустом `activeSceneId` выходил, **не сбросив `isPlaying`**. Дальше любой старт отбивался прекондишеном «Game is already running» — стейдж чёрный до конца сессии, и агент верифицирует несуществующую игру | Неудачный старт откатывает play-режим (`abortFailedStart`) и помечается флагом, чтобы его же стоп не стёр баннер с причиной |
| Сцена молча не открывалась, а play-режим всё равно включался | `ensureSceneActive` игнорировал, что `CommandDispatcher.execute` при заблокированных прекондишенах **возвращает `false`, а не бросает**; обе `Start*GameCommand` дальше флипали play-режим | `ensureSceneActive` бросает, если сцена не стала активной; обе команды перед флипом проверяют `activeSceneId`; стейдж Flow повторяет запуск до 4 раз с шагом 500 мс, потому что первый запуск гоняется с бутстрапом проекта |
| Первый ход агента исчез из чата на 3-м шаге — пустой чат, в истории тоже пусто | `ensureLoaded` проверял «идёт ли ход» **только до своих await'ов**. Панель чата монтируется для свежесозданного проекта ровно тогда, когда бутстрап шлёт его первый ход; чтение истории резолвилось вторым и затирало ход (незавершённый ход в историю ещё не попал — терялся насовсем) | Проверка повторяется после каждого await: кто сдвинул беседу, пока мы читали, тот и прав |

**Замер после фиксов** (чистый прогон с hero, тот же промпт): предыдущая игра гаснет на 8.3 с
(планер), **первый играбельный кадр нового проекта — 12.2 с**, баннера нет, ход агента жив.
Критерий 1 фазы 1 закрыт с запасом; остальные пункты критерия проверяются тем же прогоном.

Регрессии: `play-workspace.spec.ts` (4 теста на `ensureSceneActive`) и тест в
`AgentChatService.spec.ts` на «медленная загрузка истории не затирает начавшийся ход».

## 11.8 Темп генерации: замер, три меры, повторный замер (2026-08-13)

Пользовательский эталон — Canvas в чате Gemini: один промпт, короткое ожидание, играбельный
прототип. Замер нашего цикла на том же промпте («тапалка про монетки», `recipe-tapper-2d`,
Sonnet 5 через мост) дал разложение времени **одного** инкремента: 55 обращений к модели, 54
вызова тулов, **459 с модельной латентности** (~9 минут wall-clock) при том, что бутстрап
доводит до играбельного скелета за 12 с и в проблему не входит.

Куда уходило: разведка ~22 хопа / ~170 с (37 %), церемония проверки ~19 хопов / ~179 с (39 %),
собственно правки 9 хопов / ~101 с (22 %). Сквозные множители: рост контекста 21K→100K
(~0.11 с на 1K токенов на хоп ⇒ ~185 с) и пол латентности моста. Замер моста напрямую (171
токен, ответ в одно слово): **холодная сессия 5–10.5 с, тёплое продолжение 1.95–3.5 с** — то
есть 55 хопов платят 110–190 с только за транспорт. Ассистентские сообщения содержали строго
один вызов тула (`{0:1, 1:54}`) — батчинг не запрещён нашим циклом, его не делает сама лана.

Сделано три меры (анализ и ранжирование — Fable):

1. **Карта проекта в первом (кэшируемом) сообщении** — `buildProjectMap` инлайнит все скрипты и
   сцены свежесозданного проекта байт-в-байт. ~10K токенов один раз против десяти `fs_read`
   по 10 с каждый; байт-в-байт принципиально, потому что это якоря для `str_replace`.
2. **Effort доезжает до модели.** Редактор всегда слал `output_config.effort`, мост его молча
   ронял. Теперь `sessions.ts` форвардит его в `query({ effort })` (у SDK ровно те же уровни),
   а несовпадение effort'а разводит запросы по разным сессиям. Flow по умолчанию просит `low`
   (`defaultReasoningEffort`), Studio оставляет модельный дефолт.
3. **Скриншоты закрыты харнессом, а не текстом скилла:** пока висит недоказанная правка
   геймплея, `viewport_screenshot`/`analyze_image` отвечают отказом со ссылкой на
   `game_input`/`game_observe`; для честно визуального вопроса есть параметр `visualReason`.

**Повторный замер, тот же промпт и рецепт:**

| | до | после |
| --- | --- | --- |
| Обращений к модели | 55 | **26** |
| Модельное время | 459 с | **145 с** |
| Медиана шага / худший шаг | 5.4 с / 68.9 с | **4.2 с / 15.0 с** |
| Контекст в конце хода | 99.5K | **57.2K** |
| `fs_read` за ход | 10 | **0** |
| Разведочных хопов | ~22 | **2** |
| Ход целиком | ~9 мин | **~3 мин** |

Оговорка: прогоны не идентичны по объёму работы. Увидев исходники скриптов, агент заключил,
что первый инкремент рецепт уже реализует целиком, и ограничился доказательством в игре (в
базовом прогоне было 9 мутаций). Это и есть эффект меры 1, но часть выигрыша по времени идёт
именно отсюда, а не только из числа хопов. Гейт на скриншоты живьём ни разу не сработал:
долг снимается первым же успешным `game_input`, а картинки агент звал после — эффект меры 3
подтверждён только юнит-тестом.

**Побочно найденный баг:** агент переписал `design/progress.md` нумерованным списком вместо
чеклиста — трекер плана молча опустел («No plan yet») посреди хода, где агент отчитывался о
прогрессе. `parseChecklist` теперь понимает и нумерованные планы со словом-статусом
(DONE / in progress / готово), проверено живьём: план снова 1/5.

## 11.9 Змейка: замер по ходам и три фикса (2026-08-13)

Второй прогон продукта целиком, промпт «змейка» (`recipe-arena-2d`). Первый играбельный кадр —
25.4 с (дольше тапалки: планер стартовал с холодной сессии моста, открытого проекта не было).

| | ход 1 (движение по сетке + рост) | ход 2 (стены, самоукус, game over) | ход 3 (счёт) |
| --- | --- | --- | --- |
| Обращений к модели | 51 | **12** | 60 (упёрся в кап) |
| Модельное время | 270 с | **78 с** | 356 с |
| Ход целиком | ~5.5 мин | **130 с** | оборван капом |
| Контекст (вход) | 44.6K → 92.7K | 96K → 107K | 107K → **144K** |

Игра играется: движение по сетке в 4 направлениях, рост хвоста, перестановка еды, запрет
разворота на 180°, смерть о стену и о хвост, GAME OVER с финальным счётом и RETRY. Проверено
не агентом, а отдельным автопилотом через `game_observe`/`game_input`: **SCORE 0 → 1**,
сегментов 2→3.

Три бага, каждый починен:

| Симптом | Корень | Фикс |
| --- | --- | --- |
| Ход 3 сжёг весь кап в 60 итераций, гоняясь за косметикой | Агент восемь раз перетюнивал одно свойство (`moveInterval`), каждый раз с новым значением, так что проверка на **байт-идентичный** повтор не срабатывала ни разу | Loop-breaker считает не значения, а **ручку**: `tool:target:property` без значения. Четвёртая перенастройка одного свойства за ход — сигнал «дело не в значении». Только для сеттеров свойств: переписать один файл 4 раза нормально, перетюнить одно свойство 4 раза — нет |
| В сцене рядом лежали `cellSize: 70` и `moveInterval: "1.5"` | `set_component_property`/`set_property` клали значение модели как есть; JSON модели типизирован свободно, а схема свойства знает тип | `coerceToPropertyType` в `fw/property-schema.ts` (число ← числовая строка, буль ← `true`/`false`, строка ← скаляр; всё остальное не трогаем), вызывается обеими операциями **до** валидации |
| Контекст рос через ходы: 44K → 144K | Свежая беседа заводилась только на первый инкремент (бутстрап), каждый следующий промпт продолжал ту же — при латентности ~0.11 с на 1K токенов на хоп третий инкремент платил ~8 с за историю, которой не пользовался | `flowIncrementHandoff`: в Flow новый инкремент начинается с чистой беседы, если прошлый запрос был ≥ 60K токенов. В неё переносится резюме прошлого хода и заново собранная карта проекта; маленькие беседы и ответ на `ask_user` не сбрасываются |

Карта проекта вынесена в `services/flow/flow-project-map.ts` — её теперь строят и бутстрап, и
свежая беседа (иначе сброс беседы стоил бы обратного возврата к `fs_read`).

Мелочь для скилла, найденная руками: `game_input` с длинным `ms` не рулит — направление меняет
короткое нажатие (~120 мс). Агент проверял холдами 400–800 мс и разницы не заметил.

**Осталось открытым из §11.3:** рецепты по-прежнему 5.3–7.5 КБ против целевых 4 КБ (раздел про
`GameRules` их ещё удлинил — сокращать надо отдельным проходом); `hitstop` каждый кадр (не
воспроизведён изолированно); расхождение двух `tsconfig` по `useDefineForClassFields` (не стреляет,
но менять семантику сборки без прогона DeepCore не стоит); «агент объявил починенным, проверяя с
грязной доски» — общего харнесс-сигнала так и нет: единственное, что харнесс знает о проверяемом
условии, — это `expect`, а он про дельты движения, поэтому эвристику «`before` уже удовлетворяет»
осознанно не выдумывали. Правило «проверяй с чистого старта» остаётся в `flow-increment`.
