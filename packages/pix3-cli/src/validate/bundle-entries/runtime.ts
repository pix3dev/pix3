// Bundle entry: the `@pix3/runtime` instance compiled user scripts import at level 2 (the same
// module instance the validator uses — esbuild's code splitting shares the chunk).
export * from '@pix3/runtime';
